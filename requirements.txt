# LangChain / LangGraph 核心
langchain==1.3.2
langchain-core==1.4.0
langchain-openai==1.2.2      # OpenAI LLM（之後換本地模型可改用 langchain-ollama 等）
langgraph==1.2.2             # Agent 與後續流程編排
langgraph-checkpoint-postgres==3.1.0  # 對話記憶持久化到 Postgres（解鎖多 replica；見 base_agent.get_checkpointer）
langchain-mcp-adapters==0.3.0  # 經 MCP 接可攜式 agent（ppt-agent）的工具

# 工具相依
pypdf==6.12.2               # PDF 讀取轉文字（markitdown 轉換失敗時的後援）
markitdown[pdf,docx,pptx,xlsx,xls]==0.1.6  # RAG 讀檔：Excel/Word/PPT/CSV/PDF… 統一轉 Markdown 供建索引
python-pptx==1.0.2          # 簡報（.pptx）產出
psycopg[binary]==3.3.4      # PostgreSQL 驅動（sql agent 查詢人事資料）
psycopg-pool==3.3.1         # 連線池（checkpointer 對 Postgres 的可寫連線，併發安全）
boto3==1.43.26             # 物件儲存（MinIO/S3）：/files 從 bucket 串流產物；不設 S3 env 不會用到
PyJWT==2.13.0              # 登入：簽/驗自家 JWT（Google 登入後的 session token）

# 後端 API（前端透過它呼叫 agents / 流程）
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart==0.0.29

# 環境變數
python-dotenv==1.2.2
